{
  "name": "liete-platform",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "dev": "corepack pnpm tokens:build && corepack pnpm --filter @liete/web dev",
    "build": "corepack pnpm -r build",
    "typecheck": "corepack pnpm -r typecheck",
    "test": "corepack pnpm -r test",
    "storybook": "corepack pnpm --filter @liete/tokens build && corepack pnpm --filter @liete/ui-web storybook",
    "build-storybook": "corepack pnpm --filter @liete/tokens build && corepack pnpm --filter @liete/ui-web build-storybook",
    "tokens:build": "corepack pnpm --filter @liete/tokens build"
  }
}
